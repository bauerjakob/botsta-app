mutation RegisterUser($username: String!, $password: String!, $publicKey: String!) {
  register(username: $username, password: $password, publicKey: $publicKey) {
    token,
  	refreshToken,
    hasError
  }
}